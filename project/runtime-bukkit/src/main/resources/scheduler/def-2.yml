scheduler:
  uniqueId: "默认调度-日程"

  # 是否启用
  enable: true

  # 启动日期
  start: '2024-1-1 00:00:00'

  # 结束日期， 如果不写则不会取消，
  end: '2024-12-15 22:55:00'

  # 间隔， 如果不写则一次性
  period: '120s'

  # 发送给所以在线玩家
  target: online

  # 需要发送的模板
  template: 每两分钟奖励

  # 自定义指令
  command: []



